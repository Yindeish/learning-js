Questions
1.

Create a type for updating a user where:

Only name and email can be updated
Both fields should be optional
2.

Create a type for displaying user info in a profile card:

Include only name and email
Both should be required
3.

Create a type for a login function:

Only email and password
No extra fields allowed
4.

Create a type for partially updating any user field:

All properties should be optional
5.

Create a type for admin view:

Include only id, name, and isAdmin
All fields required
6.

Create a type for editing user permissions:

Only isAdmin
Optional (you may or may not update it)
7.

Create a type for sending a public user list:

Only id and name
Required fields
8.

Create a type for updating login credentials:

Only email and password
Both optional
9.

Create a type for a function that updates ONLY the user's name:

Only name
Required (must always be provided)
10.

Create a type for a patch request where:

Only name, email, and password can be updated
All should be optional