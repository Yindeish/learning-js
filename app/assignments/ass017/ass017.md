Here’s a clean continuation in the exact same structure and difficulty progression as your previous assignment.

---

# Assignment 017

## Questions

1.

Create a base `User` type and another type that:

Extends it to include `isAdmin`
All fields should be required

---

2.

Create a type for a product where:

`status` can only be `"available"` or `"out_of_stock"`
No other values allowed

---

3.

Create two types:

`Student` with `name` and `grade`
`Teacher` with `name` and `subject`

Then create a type that can represent either a student or a teacher

---

4.

Create a type for a strict user role where:

Role can only be `"admin"`, `"user"`, or `"guest"`

---

5.

Create two types:

`Address` with `street` and `city`
`Contact` with `email` and `phone`

Merge them into a single type

---

6.

Create a type for grouping users where:

It should be an object with keys as group names (string)
Values should be an array of users

---

7.

Create a type for a list of products where:

Each product has `id` and `name`
The list should be an array of that type

---

8.

Create a type for a blog post where:

`title` is required
`description` is optional

---

9.

Create a type for a user profile where:

It contains a nested object `address`
`address` should have `city` and `country`

---

10.

Create a type for a function where:

It takes `email` and `password` as arguments
Returns a boolean

---

11.

Given a `User` type, create a new type where:

All fields are optional

---

12.

Given a `User` type, create a new type that:

Includes only `name` and `email`

---

13.

Given a `User` type, create a new type that:

Excludes the `password` field

---

14.

Create a type that represents:

A record where keys are user IDs (string)
Values are user objects

---

15.

Create a type casting example where:

You have a variable of type `unknown`
Cast it to a `string`
