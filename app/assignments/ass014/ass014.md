**Assignment 014 – TypeScript (Based on Covered Topics Only)**

---

### 1. Student Management System (Object Types + Array of Types + Nested Types)

A school application stores student records.
Each student has:

* studentId
* fullName
* age
* email
* courses (a student can register for multiple courses)

Each course has:

* courseCode
* courseTitle
* creditUnit
* lecturerName

**Task**

1. Create a TypeScript **type** for `Course`.
2. Create a TypeScript **type** for `Student`.
3. Ensure `courses` is typed as an **array of Course**.

---

### 2. Crypto Portfolio Tracker (Union Types + Object Types)

A crypto dashboard stores user assets.
Each portfolio item contains:

* assetName
* symbol
* balance
* priceUsd
* chain

Chains can only be one of:

* Ethereum
* Bitcoin
* Solana
* Polygon

**Task**

1. Create a **union type** for `Chain`.
2. Create a `PortfolioAsset` type using the chain union.

---

### 3. Messaging Application (Optional Types + Nested Types + Array of Types)

A chat system stores messages between users.
Each message contains:

* messageId
* senderId
* receiverId
* content
* attachments (optional)
* timestamp

Attachments contain:

* fileName
* fileSize
* fileType

**Task**

1. Create an `Attachment` type.
2. Create a `Message` type.
3. Ensure `attachments` is **optional** and can store **multiple attachments**.

---

### 4. E-commerce Order System (Union Types + Array of Types)

An online store tracks orders.
Each order contains:

* orderId
* customerName
* products
* totalPrice
* orderStatus

Order statuses:

* pending
* shipped
* delivered
* cancelled

Each product contains:

* productId
* name
* price
* quantity

**Task**

1. Create a `Product` type.
2. Create a union type for `OrderStatus`.
3. Create an `Order` type ensuring `products` is an **array of Product**.

---

### 5. User Profile System (Optional Types + Object Types)

A system stores user profiles.
Each user has:

* userId
* username
* email
* phoneNumber (optional)
* isVerified

**Task**

1. Create a `User` type.
2. Make `phoneNumber` optional.

---

### 6. Strict Email System (Strict Types)

Emails must follow this pattern:
`example@gmail.com`

**Task**

1. Create a strict type for Gmail using template literals.
2. Declare a variable using that type.

---

### 7. URL Validator (Strict Types)

URLs must start with:
`https://`

**Task**

1. Create a strict type for URL.
2. Assign a valid and invalid example.

---

### 8. Vehicle System (Union Types)

A system stores vehicle brands.

Allowed brands:

* Toyota
* BMW
* Mercedes
* Hyundai

**Task**

1. Create a union type for `CarBrand`.
2. Declare a variable using that type.

---

### 9. Type Merging (Intersection Types)

You are given:

* A type for login details
* A type for user profile

**Task**

1. Create `Login` type (email, password).
2. Create `Profile` type (name, age).
3. Merge both into a single type.

---

### 10. Grouping Types (Intersection + Reusability)

A system shares common fields across roles.

Common fields:

* name
* age
* country

Student fields:

* level
* department

Teacher fields:

* subject
* salary

**Task**

1. Create a `Common` type.
2. Create `Student` and `Teacher` types.
3. Combine them using **intersection types**.

---

### 11. Array of Types (Collections)

A blog stores posts.

Each post has:

* id
* title
* content
* author

**Task**

1. Create a `Post` type.
2. Create an array of posts using that type.

---

### 12. Optional Settings System (Optional + Nested Types)

A settings object contains:

* account
* notifications (optional)

Account:

* security
* emailUpdates (optional)

Notifications:

* sound
* vibration (optional)

**Task**

1. Create all required types.
2. Ensure optional fields are correctly applied.

---

### 13. Function Types

A system calculates total price of items.

**Task**

1. Create a function type that takes:

   * price (number)
   * quantity (number)
2. Returns total (number).

---

### 14. Utility Types (Partial, Pick, Omit, Record)

Given a `User` type:

* id
* name
* email
* password

**Task**

1. Use **Partial** to make all fields optional.
2. Use **Pick** to select only name and email.
3. Use **Omit** to remove password.
4. Use **Record** to map user IDs to users.

---

### 15. Template Literal Types (Advanced Strict Types)

Create a system for product IDs.

Format:
`PROD-123`

**Task**

1. Create a strict type for product ID using template literals.
2. Declare a variable using that type.

---

If you want, I can also generate **answers/solutions** or turn this into a **graded test or PDF**.
