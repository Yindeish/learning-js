# Git fetch (General purpose)

# It is used to pull latest code from from the remote (Github) and it gets code from all the branches

# Types of Remote
# 1. origin (repository) ?? Most times you'll work with this

# 2. upstream (a reposiotry you forked from) // forget this for now // is because when you want to contribute to open-source projects on Github


# Git pull 

# git pull is used to pull from a particular branch
# git pull origin branchName
